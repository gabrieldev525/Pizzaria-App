<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.unifor.br.pizzadelivery">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PizzaDelivery">
        <activity
            android:name=".views.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.RegisterActivity">
        </activity>
        <activity
            android:name=".views.PizzaListActivity">
        </activity>
        <activity
            android:name=".views.OrderCompleteActivity">
        </activity>
        <activity
            android:name=".views.AddressStepOrderActivity">
        </activity>
        <activity
            android:name=".views.PaymentStepOrderActivity">
        </activity>
        <activity
            android:name=".views.PizzaDetailActivity">
        </activity>
    </application>

</manifest>